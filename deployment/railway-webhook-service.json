{
  "name": "plinto-webhook-service",
  "source": "apps/webhook-service",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  },
  "variables": {
    "PORT": "8003",
    "PLINTO_ENV": "production",
    "JWT_AUDIENCE": "plinto.dev",
    "JWT_ISSUER": "https://plinto.dev"
  },
  "domains": [
    {
      "domain": "webhooks.plinto.dev"
    }
  ],
  "networking": {
    "serviceName": "plinto-webhook-service",
    "internalPort": 8003
  }
}